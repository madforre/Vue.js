구글지도 API(구글 클라우드)와 뷰 라우터 이용, 값을 연관배열로 바꿔서 
위치까지 저장하기 + 배포

17 - 배포

input에서 장소 검색 -> 검색된 대표 장소들 랜더링 -> 
원하는 장소 클릭 -> 해당 장소의 경로 반환 -> 
지도상에 표시 -> 저장 -> 메모 클릭 -> 
숨겨져있던 구글지도 나오면서 해당 메모의 지도관련 JSON (위도, 경도, 위치) 가져옴 -> 
위도, 경도 Maps API에 제공 -> 화면 랜더링

약속 1,2,3,4,5 중요도 관련하여 D3.js으로 작성해보기
